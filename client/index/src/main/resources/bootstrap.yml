spring:
  cloud:
    config:
      name:   index,eureka,database
      label: master
      discovery:
        enabled: true
        service-id: config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka